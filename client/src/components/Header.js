// Import statements
import React, { Component } from 'react';
import '../App.css';

// Create the component
class Header extends Component {
    render() {
        return(
            <p className = 'header'>Welcome to Startup World</p>
        )
    }
}

// Export components
export default Header;