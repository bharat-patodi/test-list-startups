import React, { Component } from "react";

export default class CreateStartup extends Component {
    render() {
        return (
            <div>
                <p>Create a Startup Component!</p>
            </div>
        );
    }
}